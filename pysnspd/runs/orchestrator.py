"""Full workflow orchestration helpers."""


def run_full_workflow(config, run_name):
    """Run PRE-run, SS-run, and PHOTON-run in sequence."""
    return 0


def check_stage_dependencies(config, run_name, target_stage):
    """Check that previous stages exist before launching a target stage."""
    return 0
