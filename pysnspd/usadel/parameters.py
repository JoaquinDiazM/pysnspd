"""Material-parameter calibration for dirty-limit superconductivity."""


def derive_dirty_limit_parameters(material_config):
    """Compute derived material scales used by Usadel and gTDGL."""
    return 0


def estimate_critical_current(material_config, geometry_config, usadel_config):
    """Estimate the depairing or critical current from a Usadel sweep."""
    return 0


def calibrate_diffusion_from_bias(material_config, bias_config, geometry_config):
    """Calibrate or select the diffusion coefficient for the bias state."""
    return 0
