"""Data-loading helpers for plotting."""


def load_run_data(config, run_name, stage):
    """Load raw data for plotting without modifying it."""
    return 0


def load_plot_manifest(config, run_name):
    """Load the plot manifest for a run."""
    return 0
