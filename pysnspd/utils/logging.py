"""Logging helpers for runs and pipelines."""


def setup_run_logger(config, run_name, stage):
    """Create a logger associated with one run and one stage."""
    return 0


def log_stage_summary(logger, summary):
    """Write a compact summary of a completed stage."""
    return 0
