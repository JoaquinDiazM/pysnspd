"""Parallel-execution helpers."""


def get_parallel_context(parallel_config):
    """Create or describe the requested parallel execution context."""
    return 0


def map_tasks(tasks, worker_function, parallel_config):
    """Map independent tasks over the selected parallel backend."""
    return 0
