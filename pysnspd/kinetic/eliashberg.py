"""Eliashberg spectral data and phonon-density utilities."""


def load_alpha2F(path):
    """Load the Eliashberg spectral function ``alpha^2 F(Omega)``."""
    return 0


def load_phonon_dos(path):
    """Load the phonon density of states ``F(Omega)``."""
    return 0


def build_debye_reference_spectrum(material_config, omega_grid):
    """Construct a Debye reference spectrum for validation and limiting tests."""
    return 0
