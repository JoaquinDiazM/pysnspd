"""Supercurrent, normal current, and total-current utilities."""


def compute_supercurrent(state, mesh, material_fields, usadel_catalog):
    """Compute the superconducting current field."""
    return 0


def compute_normal_current(state, mesh, material_fields):
    """Compute the normal Ohmic current field from the scalar potential."""
    return 0


def compute_total_current(supercurrent, normal_current):
    """Compute total current and derived diagnostics."""
    return 0
